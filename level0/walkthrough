level0:level0

Le fichier level0 est présent a la racine de l'utilisateur

$>pwd
/home/user/level0

$>ls
level0

$>./level0
Segmentation fault

$>./level0 0
No !

$>./level0 10000000000000000
No !

J'ai donc désassemblé le programme et j'ai cherché le main()

Une fois désassemblé on peut y voir le code dans le fichier Ressources/pseudo_assembler.asm

On y voit également (0x1a7 == atoi(argv[1]))
Cette condition donne acces a la suite du programe et execute l'exploit

il faut donc executer la commande suivante:

$>./level0 423

On arrive donc sur un terminal bash en tant que "level1".